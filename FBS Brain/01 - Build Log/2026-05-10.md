# Build Log — 2026-05-10 (Session 14)

## What We Built

### Verified checkmark on Community member list ✅
- `profiles_safe` Supabase view updated to expose `manually_verified` column (new columns must go at the END of the SELECT list — lesson learned)
- `CommunityMember` type in `communityData.ts` updated: added `manually_verified?: boolean` and `reflectionMilestone?: number`
- `CommunityScreen.tsx` — added `BadgeCheck` import, mapped `manually_verified` from query, renders blue checkmark inline next to `@username` for verified members

### Verified checkmark on Public Profile screen ✅
- `PublicProfileScreen.tsx` — added `BadgeCheck` next to `@username` in the pill, matching the community list

### iOS safe area padding for onboarding ✅
- `OnboardingScreen.tsx` — all 4 `pt-14` containers that wrap `<ProgressDots>` updated to use `calc(env(safe-area-inset-top, 0px) + 3.5rem)` inline style
- Fallback `0px` ensures Android/desktop are unaffected
- Applies to: details step, TourCard wrapper, tour2a step, tour2 step

### Church logo replaces FBS logo on Public Profile ✅
- `PublicProfileScreen.tsx` — removed hardcoded FBS background + white logo images from the `@username` pill
- Added `useQuery` to fetch `logo_url` from `churches` table using `churchId` prop
- Fallback: first letter of church name in accent-colored circle — never FBS branding
- Works for every church: community is church-scoped so `profile.church_id` always matches the viewed member's church

## Files Changed
- `src/components/fbs/communityData.ts`
- `src/components/fbs/CommunityScreen.tsx`
- `src/components/fbs/PublicProfileScreen.tsx`
- `src/components/fbs/OnboardingScreen.tsx`
- Supabase SQL: `profiles_safe` view updated

## Lesson Learned
`CREATE OR REPLACE VIEW` in Postgres does not allow inserting new columns in the middle of an existing view — they must go at the END. Adding `manually_verified` before `phone_number` caused an error. Fixed by moving it to the end.

## What's Next
- Churchless signup investigation — prayer/give pages stay locked after manually joining a church mid-session
- White-label sign-up screen — auth screen reads `?church=` from URL, shows church logo + name, auto-links member on signup
- QR code → church landing page flow (V1 critical)
