# Build Log — 2026-05-01

## What We Built

### DB Cleanup
- Dropped `follows` table from Supabase
- Fixed `profiles` RLS policy that referenced `follows` in its USING clause — removed the "followers can see private profiles" subquery
- Changed username uniqueness constraint from global (`username`) to church-scoped (`church_id, username`) — permanent architectural decision, closes door on cross-church user discovery forever

### Onboarding Overhaul
- Removed age verification step entirely — was an active confirmation screen, now replaced by passive fine print
- Removed church search step entirely — members always arrive via QR/invite URL, church context comes from URL not user selection
- New flow: **details → username → tour1 → tour2a → tour2 → tour3 → tour4 → tour5** (8 steps, down from 10)
- `church_id` now read from URL param (`?church_id=xxx`) when creating profile — architecture is ready for QR flow
- Cleaned up dead imports, `ChurchEntry` interface, church search state + useEffect (~190 lines deleted)

### AuthScreen
- Added age confirmation fine print to sign-up form: *"By signing up, you confirm you are 13 or older."* — appended to existing Terms/Privacy line
- Standard COPPA compliance pattern (same as Instagram, TikTok, etc.)

## Commit
`f54d1fe` — pushed to main, Vercel auto-deploying

---

## Session 2 — Streak Logic

### Streak Logic — DONE
- Built `update_streak_on_journal_insert()` Postgres function + trigger on `journal_entries`
- On every INSERT: checks if user already wrote today (UTC) — skips if so; extends streak if last entry was yesterday; resets to 1 if they missed a day or this is their first entry
- Updates both `streak_current` and `streak_longest` on `profiles`
- Built `award_streak_badge()` Postgres function + trigger on `profiles`
- Fires when `streak_current` hits 3, 7, 30, or 100 → inserts row into `user_badges`
- Real-time banner + confetti already wired in `Index.tsx` — no frontend changes needed
- Verified end to end: wrote entry → streak_current = 1; manually set streak_current to 3 → badge banner fired + badge appeared on profile

### Key DB objects created (via SQL Editor, no migration file):
- Function: `public.update_streak_on_journal_insert()`
- Trigger: `trg_update_streak_on_journal_insert` on `journal_entries`
- Function: `public.award_streak_badge()`
- Trigger: `trg_award_streak_badge` on `profiles`

## What's Next
1. **Founding Member backfill** — one-time SQL for users who signed up before the trigger
2. **Bible: Next Chapter / Previous Chapter buttons**
3. **Instagram handle: enforce lowercase on input**
4. **Locked badge tooltip** — tap gray badge → shows how to earn it
5. **White-label copy audit** — replace all "Faith Beyond Sundays" strings with `{churchName}`
6. **Accent color refresh**
7. **Pastor announcements feed**

---

## Session 3 — Badge Polish

### Founding Member backfill — DONE
- Ran one-time SQL in Supabase SQL Editor to insert `founding_member` badge for all existing profiles
- `user_badges` table confirmed: no `church_id` column (just `user_id`, `badge_type`, `earned_at`)
- Verified: count returned 1 (Sebastian's account) ✅

### Badge section reorder — DONE
- New order: **Special → Streaks → Reflections → Scripture** (was Reflections → Streaks → Scripture → Special)
- Changed in both `ProfileScreen.tsx` and `PublicProfileScreen.tsx`

### Locked badge tooltip — DONE
- Tap any grayed-out locked badge → centered overlay shows badge name, subtitle, and "How to earn" description
- Tap backdrop or X to dismiss
- Added `howToEarn` field to `BadgeTier` and `UserBadgeConfig` in `badgeConfig.ts`
- Tooltip icon uses the badge's actual color/gradient (not gray)
- Changed files: `badgeConfig.ts`, `BadgeStackGroup.tsx`, `ProfileScreen.tsx`

### "Day One" badge rename — DONE
- Renamed `founding_member` badge from "Founding Member / One of the first 250" → **"Day One / One of the first 20"**
- Reasoning: "Founding Member" implied FBS platform membership, which doesn't make sense in a white-label model. "Day One" is church-scoped — first 20 members per church.
- Updated trigger in Supabase: `award_founding_member_badge()` now checks per-church count ≤ 20 (was global count ≤ 250)
- New trigger: `trg_award_founding_member` on `profiles` AFTER INSERT
- Frontend label updated in `badgeConfig.ts`

### Key DB objects created/updated (via SQL Editor, no migration file):
- Function: `public.award_founding_member_badge()` — replaces old global-count founding member logic
- Trigger: `trg_award_founding_member` on `profiles`

## Commits
- `9930950` — reorder badge sections
- `1675c6f` — locked badge tooltip
- `4ed4ce4` — tooltip icon color fix
- `4008a58` — Day One rename + per-church trigger
