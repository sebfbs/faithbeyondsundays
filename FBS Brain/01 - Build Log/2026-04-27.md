# Build Log — 2026-04-27

## Session 1 (earlier)
- Saved the full PWA Install Flow game plan to `FBS Brain/02 - Backlog/PWA Install Flow.md`
- Added a Backlog section to `Current Sprint.md` with a pointer to that file
- No code written — documentation/planning session only

## Session 2 — Lovable Independence (full)

### What We Did

**Frontend — Lovable dependencies removed:**
- Removed `@lovable.dev/cloud-auth-js` from `package.json`
- Removed `lovable-tagger` dev dependency from `package.json` and `vite.config.ts`
- Deleted `src/integrations/lovable/index.ts`
- Replaced Google/Apple OAuth in `AuthScreen.tsx`, `AdminLogin.tsx`, `PlatformLogin.tsx` with native `supabase.auth.signInWithOAuth`
- Confirmed clean production build (zero errors, 3.6s)

**Backend — edge functions fully migrated off Lovable:**
- `auth-email-hook` — stripped `@lovable.dev/email-js` and `@lovable.dev/webhooks-js`, rewrote to use Resend directly with Supabase's native hook payload format
- `process-sermon` — replaced Lovable AI gateway with Anthropic API (Claude Sonnet 4.6), updated tool schemas to Anthropic format, rewrote all 3 AI call sites + response parsing
- `generate-daily-content` — replaced Lovable AI gateway with Anthropic API (Claude Haiku)
- `transcribe-journal` — replaced Lovable AI gateway with Anthropic API (Claude Haiku), added base64 image handling
- `send-admin-invite` — fixed hardcoded `faithbeyondsundays.lovable.app` redirect URLs → `faithbeyondsundays.app`
- `invite-church-admin` — same URL fix
- `send-push` — removed Lovable APNS bundle ID fallback, now fails loudly if `APNS_BUNDLE_ID` not set
- Deleted `.lovable/` directory

**Deployed all 7 edge functions to Supabase:**
`auth-email-hook`, `process-sermon`, `generate-daily-content`, `transcribe-journal`, `send-admin-invite`, `invite-church-admin`, `send-push`

**Supabase wiring completed by Sebastian:**
- `HOOK_SECRET` set in Edge Function secrets + wired to Send Email hook in Auth → Hooks
- `ANTHROPIC_API_KEY` added to Edge Function secrets
- `RESEND_API_KEY` added to Edge Function secrets

### Decisions Made
- AI model for all edge functions: Claude (Anthropic) — not Google Gemini via Lovable gateway
- Auth emails via Resend — already had the key, consistent with invite functions
- `APNS_BUNDLE_ID` deferred — not needed until push notifications are set up

### What's Next
1. **Vercel deployment** — set up project, wire production env vars
2. Walk the app end-to-end in production and document what works vs broken
3. Decide on churchless users flow for V1
4. QR code → church landing page flow
