# Build Log — 2026-05-08 (Session 12)

## What We Built

### Email Infrastructure — DONE ✅
- Deployed `auth-email-hook` edge function to new Supabase project (`xjyffcsnhlnrvnxreoft`)
- Diagnosed and fixed authorization issue: Supabase does NOT send a Bearer token when calling hooks — removed HOOK_SECRET check entirely
- Switched sending domain from `welcome.faithbeyondsundays.com` → `mail.faithbeyondsundays.com` (verified via GoDaddy auto-configure in Resend)
- All 4 email templates (signup, recovery, magic-link, invite) updated with church branding — logo + church name pulled from `churches` table via `user_metadata.church_code`
- Subject lines updated: signup = "Activate your account"

### Auth Screen — DONE ✅
- Removed Apple sign-in from welcome screen and sign-in screen
- Replaced "Forgot password" reset flow with magic link (`signInWithOtp`) — no more password reset emails
- Removed `signUpSuccess` state and "Check your email" screen — users go straight into app after signup
- Fixed `emailRedirectTo` to include `?church=${churchCode}` when church code is in localStorage

### Supabase Config — DONE ✅
- "Confirm email" toggled OFF (Auth → Providers → Email) — users go straight into onboarding after signup, no email gate

### Verified Profile Badge — DONE ✅
- `BadgeCheck` icon appears next to @username on profile page when `email_confirmed_at` is not null
- "Verified Profile" badge added to SPECIAL section in badges — grayed/locked when unverified, tappable modal with "Verify Profile Now" CTA that sends magic link
- **Known issue:** `email_confirmed_at` is auto-set by Supabase when confirm email is OFF, so all new users appear verified immediately — needs `manually_verified` column fix (documented in sprint)

## Issues Found — Logged for Next Session
1. Verified badge auto-earned — needs `manually_verified boolean` column in `profiles`
2. Verified checkmark missing from Community page member list
3. Churchless signup — prayer/give pages locked after manually joining church
4. Onboarding banners cut off — needs iOS safe area padding
5. Onboarding wizard progress dots cut off — needs iOS safe area padding

## Files Changed
- `supabase/functions/auth-email-hook/index.ts`
- `supabase/functions/_shared/email-templates/signup.tsx`
- `supabase/functions/_shared/email-templates/recovery.tsx`
- `supabase/functions/_shared/email-templates/magic-link.tsx`
- `supabase/functions/_shared/email-templates/invite.tsx`
- `src/components/fbs/AuthScreen.tsx`
- `src/components/fbs/ProfileScreen.tsx`
