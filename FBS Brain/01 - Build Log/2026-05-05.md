# Build Log — 2026-05-05 (Session 8)

## What Was Built

### White-Label Copy Audit — COMPLETE ✅

Full audit of the member-facing app and admin dashboard for hardcoded Faith Beyond Sundays branding. Every instance removed or made dynamic.

**Files changed:**

| File | Change |
|------|--------|
| `src/components/fbs/AuthScreen.tsx` | Removed FBS logo, "Faith Beyond Sundays" title, and "Try the Demo" link from member welcome screen. Now shows neutral tagline only. |
| `src/components/fbs/HomeTab.tsx` | Empty state: `"Welcome to Faith Beyond Sundays"` → `"Welcome to {churchName}"` (dynamic from DB, fallback: "your church") |
| `src/components/fbs/WelcomeScreen.tsx` | Removed FBS logo and title from legacy welcome screen |
| `src/pages/admin/AdminLogin.tsx` | Subtitle: `"Manage your church on Faith Beyond Sundays"` → `"Sign in to manage your church"` |
| `src/components/fbs/TabletSidebar.tsx` | Logo alt text: `"FBS"` → `"Church logo"` |
| `src/components/fbs/PublicProfileScreen.tsx` | Logo alt text: `"FBS"` → `""` (decorative) |

**Key decisions made this session:**
- Admin dashboard (option B): FBS brand stays in settings/footer only. Headlines and main copy cleaned up.
- Auth welcome screen: FBS logo removed entirely. Church logo will live here once QR code + logo upload sprint is built.
- "Try the Demo" button removed from member sign-up screen — confusing for congregation members arriving from a church QR code.
- `churchName` was already wired through `useProfile()` → `Index.tsx` → `HomeTab` props. No new fetches needed.

**Not touched (intentional):**
- `Terms.tsx` + `Privacy.tsx` — FBS is the legal entity, naming is correct there
- `PlatformLogin.tsx` — Platform owner panel, FBS brand is appropriate
- `AuthProvider.tsx` — No changes needed; `useProfile()` already joins church name

---

## Decisions & Context

**Platform owner dashboard exists** (`/platform`): full church management already built — create church, assign admin email, send invite, activate/deactivate, delete. No church logo upload yet — planned as next sprint item.

**White-label sign-up screen spec saved** to `Current Sprint.md` backlog: once QR code + church logo upload are built, the auth screen will read `church_id` from the URL and show that church's logo and name dynamically.

---

## What's Next (Priority Order)

1. **Platform dashboard walkthrough** — Log in to `/platform`, walk every screen with Sebastian, then add church logo upload to the New Church form
2. **Email hook re-wiring** — Resend + auth email hook broken on new Supabase project. Blocks password reset + admin invite emails.
3. **End-to-end member app walkthrough** — Walk every screen as a member, document what works vs. broken
4. **QR code → church landing page flow** — Embeds `church_id` in URL, church-branded auth screen

---

## Commit

`b80a5d1` — feat: white-label audit — remove all FBS brand from member app + admin headlines
