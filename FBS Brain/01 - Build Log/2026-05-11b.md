# Build Log — 2026-05-11 (Session 16)

## What We Did

### Church Landing Page — Wired Up + iOS Steps Fixed ✅

**Problem:** `/?church=overflow` was routing straight into the app, skipping the landing page entirely. First-time visitors never saw install instructions.

**Fix:** One-file change to `ChurchGateway.tsx`:
- `?church=` param → show `ChurchLandingPage`
- No `?church=` but church in localStorage → go straight to the app (fixes returning users hitting `/` without a param)
- Neither → show `FindChurchScreen`

**iOS steps rewritten** based on Sebastian's real-device testing. Old instructions said "tap the Share button at the bottom of Safari" (wrong icon). Correct flow:
1. Tap **···** at the bottom right of Safari
2. Tap **"Share"** from the menu
3. Scroll down and tap **"View More"**
4. Tap **"Add to Home Screen"**
5. Tap **"Add"** — open the app from your home screen

**Desktop message** cleaned up — removed QR code mention (irrelevant since they're already on the URL).

### Debugging Note
The preview server had been started from a leftover git worktree (`.claude/worktrees/eager-hoover-a61507/`), so edits to the main project weren't being served. Had to stop and restart the server from the correct directory. Terminal was also inside the worktree — Sebastian needed to `cd` to the main project dir before pushing.

## Files Changed
- `src/pages/ChurchGateway.tsx` — routing logic
- `src/pages/ChurchLandingPage.tsx` — iOS steps + desktop message

## Commit
`ef47cb9` — feat: wire up church landing page with correct iOS PWA install steps

## What's Next
- Test on real iPhone in Safari at `[vercel-url]/?church=overflow`
- Deploy `send-church-request` Edge Function (still blocking the "Don't see your church?" form)
- Email hook setup (blocks password reset + admin invite emails)
